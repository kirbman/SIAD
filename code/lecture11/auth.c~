#include <stdio.h>

int main(int argc, char *argv[]) {
	char *data = getenv("QUERY_STRING");
	char username[100];
	char password[100];
	sscanf(data, "username=%[^&]&password=%s", username, password);
	/*if (username != "my" || password != "name") {
		printf("username or password is incorrect\n");
	} else {
		printf("you did it\n");
	}*/
}
